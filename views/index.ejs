<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Post</h1>
    
    <% for(let post of posts) { %>
        <div class="post">
            <h3 class="username">@<%= post.username %></h3>
            <h4 class="content"><%= post.content %></h4>
            <a href="/posts/<%= post.id %>">See post in details</a>
            <a href="/posts/<%= post.id %>/edit">Edit</a>
            <form action="/posts/<%= post.id %>?_method=DELETE" method="POST" style="display:inline;">
                <button style="color: red;">Delete</button>
            </form>
        </div>
    <% } %>

    <a href="/posts/new">Create new post</a>

    <footer style="margin-top: 50px; text-align: center; color: gray;">
        <p>Made with ❤️ by Tushar &copy; <%= new Date().getFullYear() %></p>
    </footer>
</body>
</html>
